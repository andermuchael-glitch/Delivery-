{
  "name": "entrega365-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "mobile:android": "npm run mobile:prepare && npx cap sync android && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/assets": "^3.0.5"
  }
}
